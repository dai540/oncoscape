Installation and first run
==========================

Install the lightweight environment, then run the numbered scripts with ``configs/breast_hpc.yaml``.

This repository does not build the spatial model itself. It defines the HPC execution contract.
